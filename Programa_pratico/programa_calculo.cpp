#include <iostream>
#include <iomanip>
#include <limits>

using std::cout;
using std::endl;
using std::cin;

int main(void)
{
    int Valor1, Valor2;
    
    cout << "Entre o primeiro valor: "; cin >> Valor1;
    cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');
    cout << "Entre o segundo valor: "; cin >> Valor2;
    cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');
    
    do{
        cout << "Insira qual opcao de calculo gostaria de seguir? "   
       
        
    }while(opcao = 5);
}
